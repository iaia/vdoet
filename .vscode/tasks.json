{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "pub build",
            "type": "shell",
            "command": "pub build",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "pub start",
            "type": "shell",
            "command": "pub serve",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}